{
  "name": "agripool",
  "version": "1.0.0",
  "description": "AgriPool: Web for Transport Sharing of Agricultural Produce to Markets",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install --legacy-peer-deps"
  },
  "keywords": ["agriculture", "transport", "sharing", "fertilizer", "planning"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

